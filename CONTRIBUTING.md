# 贡献指南

感谢你关注 EvoClass。项目当前处于设计与开发准备阶段，欢迎参与需求讨论、技术验证和实现工作。

## 提交 Issue

提交问题或建议时，请尽量包含：

- 使用场景与目标用户；
- 期望行为和实际行为；
- Windows 版本、显示器/DPI、触控设备等环境信息；
- 可复现步骤、截图或日志（如适用）；
- 是否会影响课堂展示、数据安全或应用启动。

请勿在公开 Issue 中提交学生名单、班级档案或其他个人信息。

## 开发约定

- 以 `main` 为稳定开发基线；
- 功能分支使用 `feat/<name>`，修复分支使用 `fix/<name>`；
- 提交信息应简短、明确，并说明变更目的；
- 领域规则优先编写单元测试；
- UI 与 Windows 集成功能需说明已验证的系统版本和 DPI 场景；
- 不提交生成目录、个人配置、签名证书、密钥或真实班级数据。

## Pull Request

Pull Request 应包含：

1. 变更内容与原因；
2. 对用户或开发者的影响；
3. 验证方式与结果；
4. 相关 Issue；
5. UI 变更截图（如适用）。

提交前请确保相关构建、测试和格式检查通过。

## 设计原则

- 离线优先，核心功能不依赖网络；
- 常驻但低干扰，不抢占课件焦点；
- 配置界面与课堂高频操作分离；
- 临时变化使用日期覆盖，不污染基础规则；
- 数据可解释、可备份、可迁移、可恢复；
- 保持独立设计与独立实现，不复制第三方受保护资产。

